# Що робити далі — покроково

Чекліст для автора проєкту. Станом на **02.09.2026**.

Подача зібрана повністю: продукт, репозиторій, відеопітч і посилання в заявці.
Жодного пункту, який блокує, не лишилось — нижче тільки календар, постійні
правила й перелік необовʼязкових доробок.

> Цей файл — робочий, не частина подачі. Перед фіналом можете видалити його:
> `git rm NEXT_STEPS.md`.

---

## Що вже закрито

- [x] **Заявку подано** 26.08, реєстрацію підтверджено 31.08.
- [x] **Питання організаторам** про розробку до 01.09 — поставлено через Discord
      02.09. Чернетку в Gmail видалити, щоб не надіслати те саме вдруге.
- [x] **Репозиторій публічний**: <https://github.com/Redpage1989/SilpoPantry>,
      підписаний тег `v1.0-submission`, у git лише `.env.example`.
- [x] **Відеопітч**: <https://youtu.be/QIGg6xPPIQs> — 4:06, «Не для всіх»,
      зібраний під дев'ять пунктів журі, 18 таймкодів в описі.
      Посилання стоїть у заявці.
- [x] **Сторінка на сайті**: <https://im.pl.ua/apps/silpo-pantry/> — опис,
      той самий ролик і посилання на демо.
- [x] **Токен «Сільпо» продовжено до 02.10.2026** (див. нижче).
- [x] Скріншоти в README.

**Попередні ролики переведено в «Приватне»** — щоб журі не натрапило на застарілу
версію: `Qlqv54VQ8dM` (пітч 4:16), `bWYUi8qHlaw` (пітч 4:03), `IpqmYcnsPuM` (тур 3:42), `1LLFg-A6KWk`
(туторіал-Short). На каналі публічно лишився один актуальний.

---

## Календар

| Дата | Подія | Наслідок, якщо пропустити |
|---|---|---|
| ~~31.08.2026~~ | ~~останній день реєстрації~~ | пройдено, підтверджено |
| 01–14.09.2026 | вікно розробки | — |
| **14.09.2026** | дедлайн подачі відеопітча | посилання вже в заявці |
| 30.09.2026 | фінал | — |
| 02.10.2026 | спливають токени «Сільпо» | це вже після фіналу |

---

## Токен «Сільпо»: перевірку закрито

Раніше тут стояв окремий крок на 05.09 — перевірити, чи спрацює автоматичне
продовження. **Перевірено достроково 02.09 примусовим оновленням**, і питання
знято:

- `token_endpoint` відповідає **HTTP 200**;
- новий термін — **30 діб** (2 592 000 с), а не три дні, як у початкового токена;
- «Сільпо» **ротує refresh-токени**: на кожне продовження видає новий, старий
  стає недійсним. У коді це враховано
  (`refreshed.refreshToken ?? session.refreshToken`).

Усі три живі сесії продовжено до **02.10.2026**. Бейдж `LIVE MCP` тримається сам.

Що варто памʼятати: через ротацію дві паралельні спроби продовжити один токен
зіпсували б одна одну. Зараз оновлення відбувається в одному місці
(`resolveAdapter` у `src/lib/mcp/index.ts`), і при невдачі рядок у базі не
змінюється — сесія лишається робочою до справжнього закінчення.

---

## Перевірка перед будь-якою подачею

```bash
cd ~/Developer/SilpoPantry
npm run verify         # typecheck + lint + 292 unit-тести
npm run test:e2e       # 13 наскрізних сценаріїв
curl -s -o /dev/null -w "%{http_code}\n" https://komora.im.pl.ua/login   # має бути 200
```

Демо тепер самодостатнє: вхід у демонстраційний режим сам наповнює комору
(9 позицій, шпинат «до завтра») і стрічку спільноти (3 рецепти, 7 голосів),
якщо вони порожні. Окремо запускати `npm run setup` на проді не треба —
`/api/dev/reset` там вимкнено назавжди.

---

## Постійні правила

За умовами конкурсу подані ідеї не є конфіденційними, але в репозиторій і
презентацію **не можна** додавати паролі, токени, персональні дані чи комерційну
таємницю. Практично:

- не показувати `.env` на екрані під час запису відео;
- не показувати реальний номер картки лояльності, телефон, адресу — в інтерфейсі
  вони й так замасковані, але слідкуйте за випадковими сповіщеннями;
- не комітити `prisma/dev.db` (уже в `.gitignore`);
- демонстраційні дані вигадані й позначені бейджем — так і лишити.

Перевірка на секрети у відстежуваних файлах:

```bash
cd ~/Developer/SilpoPantry && git ls-files -z | xargs -0 grep -lE 'sk-ant-[A-Za-z0-9]{10}|SESSION_SECRET=["'"'"']?[A-Za-z0-9]{8}|client_secret["'"'"']?[:=][^,)]*[A-Za-z0-9]{10}' 2>/dev/null || echo "✅ секретів у відстежуваних файлах немає"
```

---

## Якщо лишиться час до 14.09

Список порожній: усе, що було в ньому, зроблено. Продукт, репозиторій,
відеопітч і посилання в заявці на місці; метрики, модерація, поповнення
комори з чеків і словник закриті нижче.

Якщо зʼявиться час — найкорисніше не додавати функції, а показати продукт
живій родині й подивитись, де вона застрягне. Прототип жодного разу не був
у чужих руках, і це єдина невідомість, якої не закриє жоден тест.

---

## Словник продуктів: зроблено 02.09

Розширення 77 → 142 ключі починалося як механічне «більше продуктів», а
знайшлася вада в модерації, зробленій того ж дня.

Модерація виводить алергени з РОЗПІЗНАНОГО складу. Шістнадцять носіїв
алергенів, які `restrictions.ts` перелічує як небезпечні, у словнику були
відсутні — мигдаль, фундук, кешʼю, арахіс, тофу, креветки, манка й інші.
Інгредієнт, якого словник не знає, не тягне за собою алергену: рецепт із
мигдалем публікувався без декларації «горіхи».

Тепер це стереже тест — кожен інгредієнт з `ALLERGEN_SOURCES` мусить бути
канонічним ключем. Він і знайшов усі шістнадцять.

Перевірено на проді: рецепт із мигдалем і яйцями без декларації → чернетка,
причина «У складі є яйця, горіхи».

---

## Метрики: зроблено 02.09

Пітч на 3:45 обіцяв чотири числа, а в застосунку не було жодного. Тепер є
екран **«Що змінилось»** (`/metrics`, вхід із комори), який рахує їх із подій
самого користувача.

Правило, на якому все тримається: **не показувати відсоток, поки подій менше
трьох**. «100% страв із наявного» після однієї вечері — випадковість, і
картка каже про це прямо, замість нуля. Утримання не рахується взагалі:
місяця користування не було, і прототип не має права стверджувати нічого.

Бракувало двох джерел даних, обидва додано: `CookedMeal` (журнал
приготованого — списання змінювало комору, але не лишало сліду, якою вона
була) і `PantryItem.disposal` (з'їдено чи викинуто). Звідси нова дія
**«Викинув»** у коморі: вона відрізняється від видалення, бо `DELETE` стирає
помилку розпізнавання, а тут продукт був і зіпсувався.

---

## Імпорт чеків: зроблено 02.09

Купили ще молока — комора про це не дізнавалась, бо молоко там «уже було».
Ідемпотентність трималась на правилі «продукт уже в коморі — пропустити»,
і воно ж не давало поповнювати: два наміри в одній перевірці.

Тепер ідемпотентність тримається на журналі чеків (`receiptImport`,
унікальний `[userId, orderRef]`): обробляються лише нові чеки, а їхні
позиції додаються до наявних тим самим `planPantryWrite`, що й підтверджене
з фото. Відповідь розрізняє `imported` і `toppedUp`.

Перевірено на проді: перший імпорт — 3 чеки, додано 7 позицій, поповнено 8
(молоко 0,7 → 2,5 л, яйця 6 → 16 шт); повторний — нулі, комора не змінилась.

---

## Модерація рецептів: зроблено 02.09

Публікація була миттєвою. Тепер вона у двох частинах, бо один механізм цього
не закриває:

**До публікації** — автоперевірка (`src/lib/domain/moderation.ts`), яка блокує
лише те, що доводиться кодом: незаявлений алерген (єдина перевірка про
безпеку — на цьому списку тримається фільтр для родин з алергіями), контакти
в тексті, порожні кроки. Нерозпізнаний склад лишається попередженням: у
словнику 77 ключів, і чесна «кіноа» не має ставати причиною відмови.

Те, що не пройшло, лишається чернеткою — видно лише авторові, з причинами.
Це навмисно НЕ черга на розгляд: людини-модератора в прототипі немає, і
черга, якої ніхто не розбирає, гірша за миттєву публікацію.

**Після публікації** — скарги. Ховає третя скарга від різних людей
(`REPORTS_TO_HIDE`), повторна від того самого нічого не додає.

---

## Такт демо: зроблено 02.09

Запис починався з холодного сервера, і перші звернення до кожного маршруту
падали всередину сцен. Сцена, довша за свою репліку, стає паузою в озвучці —
найгірше діставалось «тірамісу»: 15,3 с проти цілі 4,3 с.

`scripts/record-tight.ts` тепер прогріває сім маршрутів окремим контекстом ДО
старту хронометра. Три прогони дали 171,1 / 168,7 / 168,4 с проти 178,6 с
раніше; у пітч пішов найкоротший.

| | Було | Стало |
|---|---|---|
| Тривалість пітча | 4:15,9 | **4:05,7** |
| Тиша | 21 % | **17 %** |
| Найдовша пауза | 5,7 с | **4,2 с** |

Решта перевищень — справжня тривалість дій (прокрутка, очікування
підтверджень); стиснути їх можна лише вирізавши зміст.
