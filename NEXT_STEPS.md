# Що робити далі — покроково

Чекліст для автора проєкту. Продукт готовий; усе, що лишилось, — організаційне.
Станом на **26.08.2026**.

## Уже зроблено

- [x] **Заявку подано 26.08** — за 5 днів до дедлайну. Лист-підтвердження станом
      на вечір 26.08 ще не прийшов — перевірити пошту (і спам) наступного дня.
- [x] Питання організаторам про розробку до 01.09 — чернетка лежить у Gmail,
      лишилось натиснути «Надіслати».
- [x] GitHub: <https://github.com/Redpage1989/SilpoPantry> (приватний;
      відкрити публічно перед подачею демки).
- [x] Відео-туторіал користування: <https://youtube.com/shorts/1LLFg-A6KWk>
      (не для всіх; це НЕ відеопітч — пітч на 3–5 хв ще записати за DEMO_SCRIPT.md).
- [x] Скріншоти в README.

> Цей файл — робочий, не частина подачі. Перед публікацією репозиторію можете
> видалити його: `git rm NEXT_STEPS.md`.

---

## Календар: що чим обмежене

| Дата | Подія | Наслідок, якщо пропустити |
|---|---|---|
| **31.08.2026** | останній день реєстрації | участь неможлива — це єдиний незворотний дедлайн |
| 01–14.09.2026 | вікно розробки | — |
| **05.09.2026** | спливає токен «Сільпо» | оновиться сам; якщо ні — повторний вхід за 30 секунд |
| 30.09.2026 | фінал | — |

Реєстрація закривається **до** початку вікна розробки. Тому порядок дій нижче
починається не з продукту, а з неї.

---

## Крок 1. Зареєструватися. Сьогодні — ✅ зроблено 26.08

Не відкладайте до кінця серпня: усе інше можна доробити або скоротити, реєстрацію —
ні. Якщо форма просить посилання на проєкт, дайте <https://komora.im.pl.ua> —
застосунок уже працює публічно.

- [ ] Заповнити форму реєстрації «Сільпо» AI Factory
- [ ] Зберегти підтвердження (лист / скріншот) окремо від пошти

**Заодно уточніть в організаторів одну річ.** Продукт написаний у серпні, а офіційне
вікно розробки — 1–14.09. Зазвичай попередня підготовка дозволена, але правила
бувають різні, і дізнатися це краще зараз, ніж на фіналі. Питання одним рядком:

> Чи можна подавати проєкт, розробка якого почалася до 01.09.2026?

Якщо відповідь «ні» — нічого страшного: вікно 1–14.09 стане часом на доопрацювання,
а історія комітів чесно покаже, що робилося коли.

---

## Крок 2. Опублікувати репозиторій

Зараз код лежить лише на вашому сервері (`server:/home/im/git/SilpoPantry.git`).
Журі потрібен доступ.

**Спершу перевірте, що в репозиторії немає секретів** — це пряма вимога умов:

```bash
cd ~/Developer/SilpoPantry && git ls-files -z | xargs -0 grep -lE 'sk-ant-[A-Za-z0-9]{10}|SESSION_SECRET=["'"'"']?[A-Za-z0-9]{8}|client_secret["'"'"']?[:=][^,)]*[A-Za-z0-9]{10}' 2>/dev/null || echo "✅ секретів у відстежуваних файлах немає"
```

Перевірено 06.08.2026 — чисто. Команда шукає лише справжні значення, тому порожній
плейсхолдер `SESSION_SECRET=""` у `.env.example` тривоги не викликає. Якщо колись
покаже назву файлу — не публікуйте, поки не приберете знахідку.

`.env` і `prisma/dev.db` уже в `.gitignore`, `client_id` отримується через
Dynamic Client Registration у рантаймі, тож у коді його немає взагалі.

Далі:

```bash
cd ~/Developer/SilpoPantry && gh repo create SilpoPantry --public --source=. --remote=origin --description "Сільпо: Сімейна комора — AI-агент для планування родинного меню (hackathon prototype)" --push
```

- [ ] Перевірити на GitHub, що `.env` **не** потрапив у репозиторій
- [ ] Додати посилання на <https://komora.im.pl.ua> в опис репозиторію

Публічний зробіть тоді, коли будете готові показувати. Якщо реєстрація ще не
підтверджена — створіть приватним (`--private`) і відкрийте пізніше.

---

## Крок 3. Записати відео (3–5 хв)

Сценарій із таймкодами вже написаний: [DEMO_SCRIPT.md](DEMO_SCRIPT.md).
Ціль — рівно 5:00, запасу немає.

**Підготовка перед записом:**

```bash
cd ~/Developer/SilpoPantry
npm run setup          # свіжі seed-дані: шпинат «до завтра» на місці
npm run mcp:inspect    # має показати 39 інструментів — доказ, що інтеграція жива
npm run dev
```

- [ ] Увійти через «Сільпо» — бейдж угорі має показувати `LIVE MCP`, не `DEMO MODE`
- [ ] Опублікувати 2–3 рецепти й поставити кілька голосів, інакше на 4:15 стрічка спільноти буде порожньою
- [ ] Приготувати 1–2 фото холодильника
- [ ] Вимкнути сповіщення телефона
- [ ] Записати екран 390×844 з голосом за кадром

**Два місця, які не варто вирізати з монтажу:**

Бейдж `DEMO MODE`, якщо він зʼявиться. Він не псує враження — він показує, що
застосунок ніколи не видає демо-дані за реальні.

Рядок під призом на 4:15 — про те, що це механіка, запропонована застосунком, а не
підтверджена акція «Сільпо». Виглядає як дрібниця, але саме він відрізняє робочу
інтеграцію від демо, що обіцяє неможливе. Реалістичність важить 20%.

**Якщо не вкладаєтесь у 5 хвилин** — скорочуйте такт 1:10–1:50 (фото холодильника),
а не фінальний. Приз і рецепти спільноти працюють одразу на два критерії по 25%.

---

## Крок 4. Перевірити перед подачею

```bash
cd ~/Developer/SilpoPantry
npm run verify         # typecheck + lint + 236 unit-тестів
npm run test:e2e       # 8 наскрізних сценаріїв
curl -s -o /dev/null -w "%{http_code}\n" https://komora.im.pl.ua/login   # має бути 200
```

- [ ] Усі тести зелені
- [ ] Продакшн відповідає 200
- [ ] Відео 3:00–5:00, звук чутно, текст на екрані читається
- [ ] Репозиторій публічний і відкривається в режимі анонімного перегляду

---

## Крок 5. 1 вересня — одна перевірка

Токен «Сільпо» спливає 05.09. Оновлення автоматичне (refresh-токен збережений,
логіка є в `src/lib/mcp/index.ts`), але спрацює воно чи ні — стане відомо лише
того дня, бо refresh-токени теж мають свій строк.

- [ ] Відкрити <https://komora.im.pl.ua> і подивитися на бейдж угорі
- [ ] Якщо `LIVE MCP` — нічого робити не треба
- [ ] Якщо `DEMO MODE` — натиснути «Увійти через «Сільпо»» ще раз, це 30 секунд

Зробіть це **до** запису відео, якщо записуватимете у вересні.

---

## Постійні правила

За умовами конкурсу подані ідеї не є конфіденційними, але в репозиторій і
презентацію **не можна** додавати паролі, токени, персональні дані чи комерційну
таємницю. Практично це означає:

- не показувати `.env` на екрані під час запису відео;
- не показувати реальний номер картки лояльності, телефон, адресу — в інтерфейсі
  вони й так замасковані, але слідкуйте за випадковими сповіщеннями;
- не комітити `prisma/dev.db` (уже в `.gitignore`);
- демонстраційні дані вигадані й позначені бейджем — так і лишити.

---

## Якщо лишиться час до 14.09

Продукт готовий, тож це не «треба», а «можна». За спаданням користі для оцінки:

1. **Модерація рецептів спільноти** — зараз публікація миттєва. Для пілоту цього
   мало, і журі може спитати.
2. **Розширення словника продуктів** — прибирає головне обмеження користувацьких
   рецептів: нерозпізнаний інгредієнт виключає страву з підбору агента.
3. **Скріншоти в README** — репозиторій зараз текстовий; 3–4 знімки екрана
   пояснюють продукт швидше за два абзаци.

Скажіть, якщо візьмемось за щось із цього.
